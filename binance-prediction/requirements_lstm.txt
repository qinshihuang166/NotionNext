# LSTM 深度学习项目依赖
# LSTM Deep Learning Project Dependencies
# ================================================

# 深度学习框架 / Deep Learning Frameworks
tensorflow>=2.12.0  # 主要深度学习框架 / Main DL framework
# 或者使用 PyTorch: torch>=2.0.0, 但本项目使用 TensorFlow

# 核心数据科学库 / Core Data Science Libraries
pandas>=1.3.0       # 数据处理 / Data manipulation
numpy>=1.20.0       # 数值计算 / Numerical computing
scikit-learn>=0.24.0  # 数据预处理和评估指标 / Preprocessing & metrics

# 技术指标库 / Technical Indicators
# 说明：本项目默认不强制依赖 TA-Lib（因为它在部分系统上需要额外的本地编译环境）。
# 我们在代码里使用 pandas/numpy 自己实现了 RSI/MACD/布林带 等核心指标。
# 如果你想使用更多现成指标，可以选择安装纯 Python 的 `ta`。

ta>=0.10.0          # 可选：技术分析指标集合 (纯 Python)
# TA-Lib (可选/不推荐给新手)：需要系统依赖，容易安装失败
# ta-lib>=0.4.0

# 可视化库 / Visualization Libraries
matplotlib>=3.4.0   # 基础绘图 / Basic plotting
seaborn>=0.11.0     # 高级可视化 / Advanced visualization
plotly>=5.0.0       # 交互式图表 / Interactive charts

# API 和 Web / API and Web
requests>=2.25.0    # HTTP 请求 / HTTP requests
python-binance>=1.0.15  # Binance API 客户端 / Binance API client

# 开发和工具 / Development and Tools
jupyter>=1.0.0      # Jupyter Notebook 支持
jupyterlab>=3.0.0   # 现代化的 Jupyter 界面
ipywidgets>=7.6.0   # 交互式小部件 / Interactive widgets
notebook>=6.4.0     # Notebook 环境

# 模型保存和加载 / Model Saving and Loading
joblib>=1.0.0       # 模型序列化 / Model serialization
h5py>=3.1.0         # HDF5 格式支持 (TensorFlow 模型保存)

# 配置管理 / Configuration Management
python-dotenv>=0.19.0  # 环境变量管理 / Environment variables
pyyaml>=5.4.0       # YAML 配置文件支持 / YAML config support

# 进度条和日志 / Progress Bars and Logging
tqdm>=4.60.0        # 进度条 / Progress bars
colorlog>=6.0.0     # 彩色日志输出 / Colored logging

# 性能优化 / Performance Optimization
numba>=0.53.0       # JIT 编译加速 / JIT compilation (optional)

# GPU 支持 (可选) / GPU Support (Optional)
# tensorflow-gpu>=2.12.0  # 如果有 NVIDIA GPU / If you have NVIDIA GPU
# 注意：TensorFlow 2.12+ 已自动包含 GPU 支持，无需单独安装 tensorflow-gpu

# 数据增强 (可选) / Data Augmentation (Optional)
# imgaug>=0.4.0  # 如果需要图像数据增强
# albumentations>=1.0.0

# ================================================
# 安装说明 / Installation Instructions
# ================================================
# 
# 基础安装 (CPU 版本):
# pip install -r requirements_lstm.txt
# 
# GPU 加速安装 (需要 NVIDIA GPU 和 CUDA):
# 1. 确保已安装 CUDA 11.8+ 和 cuDNN 8.6+
# 2. pip install -r requirements_lstm.txt
# 3. TensorFlow 会自动检测和使用 GPU
#
# 验证 GPU 是否可用:
# python -c "import tensorflow as tf; print('GPU Available:', tf.config.list_physical_devices('GPU'))"
#
# ================================================
# 推荐的安装顺序 / Recommended Installation Order
# ================================================
# 1. 创建虚拟环境 / Create virtual environment
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. 升级 pip / Upgrade pip
#    pip install --upgrade pip
#
# 3. 安装依赖 / Install dependencies
#    pip install -r requirements_lstm.txt
#
# 4. 验证安装 / Verify installation
#    python -c "import tensorflow as tf; print(tf.__version__)"
#
