# Binance API Configuration
# You can get these from https://www.binance.com/en/my/settings/api-management
# Note: For this project, you don't strictly need API keys as we only use public endpoints
# But having them may increase rate limits

BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# Optional: If you want to use a testnet environment (for practice)
# BINANCE_API_URL=https://testnet.binance.vision
