# Binance API Configuration
# 币安 API 配置
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# Note: API keys are optional for public data access
# You can still use the project without API keys for historical data
# 注：API 密钥是可选的，用于获取公开数据
# 即使没有 API 密钥，你也可以使用本项目获取历史数据
